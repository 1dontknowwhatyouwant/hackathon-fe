# Hackathon FE

Next.js App Router 기반의 해커톤 Frontend 프로젝트입니다.

## 기술 스택과 선택 이유

| 기술           | 선택 이유                                                                                                                                                                                                 |
| -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Node.js 22     | Next.js 16의 실행 기반을 팀 전체에서 통일하기 위해 사용합니다. `.nvmrc`와 PR CI에 버전을 명시해 로컬과 자동 검증 환경의 차이를 줄였습니다.                                                                |
| Next.js 16     | `src/app`의 App Router를 기반으로 페이지와 레이아웃을 구성하며, 파일 기반 라우팅과 서버·클라이언트 컴포넌트 분리를 활용할 수 있습니다. 빌드와 개발 서버 명령도 Next.js 표준 스크립트로 통일되어 있습니다. |
| React 19       | Next.js 16과 함께 컴포넌트 단위로 UI를 구성하는 기반입니다. 화면을 작은 단위로 분리하고 상태 변화에 따라 선언적으로 렌더링하기 위해 사용합니다.                                                           |
| TypeScript     | `strict` 모드와 `allowJs: false`를 적용해 개발 단계에서 타입 오류와 잘못된 데이터 사용을 발견할 수 있습니다. `@/*` 경로 별칭으로 모듈 import 경로도 일관되게 관리합니다.                                  |
| Tailwind CSS 4 | 별도의 대규모 스타일 파일 없이 유틸리티 클래스 조합으로 빠르게 화면을 구현하기 위해 사용합니다. 현재 전역 CSS와 PostCSS 플러그인 구성이 완료되어 있어 컴포넌트에서 즉시 사용할 수 있습니다.               |
| Axios          | 공용 인스턴스에서 API 기준 URL, JSON 헤더, 타임아웃을 한곳에서 관리할 수 있습니다. 이후 인증 헤더, 응답 변환, 공통 오류 처리를 인터셉터로 확장하기에도 적합합니다.                                        |
| Zustand        | React 클라이언트 컴포넌트에서 여러 화면이 공유하는 상태를 적은 보일러플레이트로 관리하기 위해 사용합니다. 현재 스토어도 상태와 변경 함수를 TypeScript 타입으로 명시하고 있습니다.                         |
| ESLint         | Next.js, TypeScript, React Hooks 규칙을 함께 적용해 코드 리뷰 전에 오류 가능성과 규칙 위반을 자동으로 확인합니다. PR CI에서도 별도 lint 단계로 실행됩니다.                                                |

## 인증 및 사용자 정보 저장 정책

인증 토큰과 화면에 필요한 사용자 정보는 보안 수준과 사용 목적이 다르므로 분리해 저장합니다.

| 데이터 | 저장 위치 | 관리 주체 | 사용 원칙 |
| ------ | --------- | --------- | --------- |
| Access·Refresh Token | 쿠키 | 백엔드 | 가능하면 `HttpOnly`, `Secure`, 적절한 `SameSite` 속성으로 발급합니다. 프런트엔드 JavaScript에서 토큰을 읽거나 복사하지 않습니다. |
| 사용자 정보 | localStorage | 프런트엔드 | `useAuthStore`를 통해 `id`, `email`, `nickname`, `profileImageUrl`처럼 화면 표시에 필요한 공개 정보만 저장합니다. |

- Axios 공용 인스턴스는 `withCredentials: true`를 사용해 API 요청에 쿠키를 포함합니다.
- 토큰을 localStorage, sessionStorage, Zustand 상태 또는 일반 JavaScript 쿠키로 저장하지 않습니다.
- localStorage의 사용자 정보는 사용자가 수정할 수 있으므로 인증·인가나 권한 판단의 근거로 사용하지 않습니다. 권한이 필요한 동작은 반드시 서버가 쿠키의 토큰을 검증해 결정합니다.
- 인증 상태를 사용하는 화면은 `hasHydrated`가 `true`가 된 뒤 사용자 유무를 판단해 서버 렌더링과 localStorage 값의 불일치를 방지합니다.
- 로그아웃할 때는 먼저 백엔드 로그아웃 API가 HttpOnly 쿠키를 만료시키고, 성공 여부에 맞춰 `useAuthStore.getState().clearUser()`로 로컬 사용자 정보를 제거합니다.
- 사용자 정보 응답 타입이 확정되면 `UserInfo` 타입을 API 계약에 맞춰 갱신하되 토큰 필드는 추가하지 않습니다.
- 프런트엔드와 API의 출처가 다르면 백엔드는 허용할 프런트엔드 Origin을 명시하고 credential 요청을 허용해야 합니다. credential 요청에서는 와일드카드 Origin을 사용하지 않습니다.
- 쿠키를 사용하는 상태 변경 요청은 백엔드에서 `SameSite` 정책뿐 아니라 Origin 검증이나 CSRF 토큰 등 서비스 구조에 맞는 CSRF 방어를 적용합니다.

## 설계 가정

기능 구현 전에 아래 표에 시스템이 정상적으로 동작하기 위해 전제하는 조건을 기록합니다. 가정이 더 이상 유효하지 않으면 관련 구현과 테스트를 함께 갱신합니다.

| ID      | 설계 가정 | 근거     | 검증 방법 | 가정이 틀릴 때의 영향과 대응 | 담당자   | 상태                    |
| ------- | --------- | -------- | --------- | ---------------------------- | -------- | ----------------------- |
| ASM-001 | `[작성]`  | `[작성]` | `[작성]`  | `[작성]`                     | `[작성]` | `검토 전 / 승인 / 폐기` |

작성 시 확인할 항목:

- 사용자 규모와 동시 요청량
- 지원 브라우저와 디바이스
- 인증 방식과 세션 만료 조건
- API 응답 형식과 하위 호환성
- 네트워크 품질과 응답 시간
- 데이터 정합성 및 최종 일관성 허용 여부
- 개인정보와 민감정보의 범위

## 공통 예외 처리 정책

예외를 개별 화면에서 임의로 처리하지 않도록 아래 기준을 합의한 뒤 기록합니다.

| 예외 분류                                         | 감지 위치 | 사용자 안내 | 로깅 정보 | 재시도·대체 동작 | 상위 전파 기준 | 담당 영역                        |
| ------------------------------------------------- | --------- | ----------- | --------- | ---------------- | -------------- | -------------------------------- |
| `[네트워크 / 인증 / 검증 / 서버 / 클라이언트 등]` | `[작성]`  | `[작성]`    | `[작성]`  | `[작성]`         | `[작성]`       | `[프런트엔드 / 백엔드 / 인프라]` |

공통 원칙 체크리스트:

- [ ] 사용자에게 노출할 메시지와 내부 오류 정보를 분리한다.
- [ ] 토큰, 비밀번호, 개인정보를 로그에 남기지 않는다.
- [ ] 복구 가능한 오류와 즉시 중단해야 하는 오류를 구분한다.
- [ ] 로딩 상태가 오류 이후에도 남지 않도록 종료 처리를 정의한다.
- [ ] 동일한 오류가 중복 알림 또는 중복 로그로 기록되지 않게 한다.
- [ ] 예상하지 못한 오류를 처리할 최종 fallback UI를 정의한다.

## 엣지 케이스 처리 계획

### 외부 API 및 네트워크

| 시나리오                    | 발생 조건 | 기대 동작 | 타임아웃                | 재시도 정책                                    | 사용자 안내 | 관측·로그 | 테스트 방법 |
| --------------------------- | --------- | --------- | ----------------------- | ---------------------------------------------- | ----------- | --------- | ----------- |
| 외부 API 응답 지연·타임아웃 | `[작성]`  | `[작성]`  | `[작성]`                | `[횟수, 간격, backoff, 재시도 가능 요청 구분]` | `[작성]`    | `[작성]`  | `[작성]`    |
| 네트워크 연결 끊김          | `[작성]`  | `[작성]`  | `[작성]`                | `[작성]`                                       | `[작성]`    | `[작성]`  | `[작성]`    |
| HTTP 4xx·5xx 응답           | `[작성]`  | `[작성]`  | `[해당 없음 또는 작성]` | `[작성]`                                       | `[작성]`    | `[작성]`  | `[작성]`    |
| API 응답 형식 불일치        | `[작성]`  | `[작성]`  | `[해당 없음]`           | `[작성]`                                       | `[작성]`    | `[작성]`  | `[작성]`    |
| 요청 제한 초과              | `[작성]`  | `[작성]`  | `[해당 없음]`           | `[Retry-After 처리 등 작성]`                   | `[작성]`    | `[작성]`  | `[작성]`    |

### 데이터 정합성과 동시성

DB 트랜잭션은 백엔드 책임 영역이지만, 프런트엔드의 중복 요청 방지와 충돌 응답 처리까지 함께 합의해 기록합니다.

| 시나리오              | 정합성 기준 | 백엔드 처리 계약                         | 프런트엔드 처리              | 사용자 안내 | 복구 방법 | 테스트 방법 |
| --------------------- | ----------- | ---------------------------------------- | ---------------------------- | ----------- | --------- | ----------- |
| DB 트랜잭션 경합·교착 | `[작성]`    | `[격리 수준, 재시도, 오류 코드 등 작성]` | `[작성]`                     | `[작성]`    | `[작성]`  | `[작성]`    |
| 동일 데이터 동시 수정 | `[작성]`    | `[낙관적 잠금, 버전 필드 등 작성]`       | `[작성]`                     | `[작성]`    | `[작성]`  | `[작성]`    |
| 중복 생성·결제 요청   | `[작성]`    | `[멱등성 키와 보장 범위 작성]`           | `[중복 제출 방지 방식 작성]` | `[작성]`    | `[작성]`  | `[작성]`    |
| 부분 성공·롤백        | `[작성]`    | `[트랜잭션 경계와 보상 처리 작성]`       | `[작성]`                     | `[작성]`    | `[작성]`  | `[작성]`    |

### 클라이언트 상태와 사용자 입력

| 시나리오                 | 기대 동작 | 상태 복구 | 사용자 안내 | 테스트 방법 | 결정 상태     |
| ------------------------ | --------- | --------- | ----------- | ----------- | ------------- |
| 빠른 연속 클릭·중복 제출 | `[작성]`  | `[작성]`  | `[작성]`    | `[작성]`    | `미정 / 결정` |
| 잘못되거나 누락된 입력   | `[작성]`  | `[작성]`  | `[작성]`    | `[작성]`    | `미정 / 결정` |
| 인증 만료 중 요청        | `[작성]`  | `[작성]`  | `[작성]`    | `[작성]`    | `미정 / 결정` |
| 새로고침·뒤로 가기       | `[작성]`  | `[작성]`  | `[작성]`    | `[작성]`    | `미정 / 결정` |
| 환경변수 누락            | `[작성]`  | `[작성]`  | `[작성]`    | `[작성]`    | `미정 / 결정` |

## 예외 및 설계 결정 기록

예외 처리 기준이나 설계 가정이 변경되면 아래 표에 변경 이유와 영향을 남깁니다.

| 날짜         | 결정 ID   | 변경 내용 | 변경 이유 | 영향 범위 | 결정자   | 관련 PR·문서 |
| ------------ | --------- | --------- | --------- | --------- | -------- | ------------ |
| `YYYY-MM-DD` | `DEC-001` | `[작성]`  | `[작성]`  | `[작성]`  | `[작성]` | `[링크]`     |

## 시작하기

의존성을 설치하고 개발 서버를 실행합니다.

```bash
npm ci
npm run dev
```

브라우저에서 [http://localhost:3000](http://localhost:3000)을 엽니다.

## 환경변수

`.env.example`을 복사해 `.env.local`을 만들고 API 주소를 설정합니다.

```bash
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
```

## 명령어

```bash
npm run dev        # 개발 서버
npm run build      # 프로덕션 빌드
npm run lint       # ESLint 검사
npm run type-check # TypeScript 검사
```

## 기본 구조

```text
src/
├─ app/             # 페이지, 레이아웃, 전역 스타일
├─ components/      # 전역 초기화 및 공용 컴포넌트
├─ lib/axios.ts     # Axios 공용 인스턴스
└─ store/           # 앱 및 인증용 Zustand 전역 상태
```
